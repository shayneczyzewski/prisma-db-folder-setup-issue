{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "studio": "npx prisma studio --schema ../db/schema.prisma",
    "migrate": "npx prisma migrate dev --schema ../db/schema.prisma",
    "generate": "npx prisma generate --schema ../db/schema.prisma"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^3.9.2",
    "express": "^4.17.2"
  },
  "devDependencies": {
    "prisma": "^3.9.2"
  }
}
